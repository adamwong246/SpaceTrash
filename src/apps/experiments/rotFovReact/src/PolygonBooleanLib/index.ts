import {
    selectUnion, combine, union, segments, polygon, selectIntersect
} from 'polybooljs';

export default {
    combine,
    polygon,
    segments,
    selectIntersect,
    selectUnion,
    union,
};
