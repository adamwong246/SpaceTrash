import * as React from "react";
import {List} from "immutable";

export default {
  shipMap: {},
  drones: false,
  terminalLines: [
    "booting spaceTrash session terminal",
  ],

  userView: false,

  autoPilot: false,
  shipYard: false,
  dashBoard: false,

  yardedShip: false,

  openSession: false
}
