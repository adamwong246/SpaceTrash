const { fromJS } = require('immutable');

module.exports = fromJS({
  gameStates: fromJS({
    // SESSION_ID: {
    //   girdData: [  (x level)
    //     [          (y level)
    //       [        (z level)
    //
    //       ]
    //     ]
    //   ]
    // }
  }),
});
